<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns:fx="http://javafx.com/fxml" 
            fx:id="rootPane"
            fx:controller="com.comp2042.controller.menu.EndlessGameOverController"
            prefWidth="900" 
            prefHeight="800"
            styleClass="endless-gameover-root">
    
    
    <!-- Top: Title Section -->
    <top>
        <VBox alignment="CENTER" spacing="5" styleClass="title-section">
            <padding>
                <Insets top="30" bottom="0" left="40" right="40"/>
            </padding>
            
            <!-- Main Title -->
            <Label fx:id="mainTitleLabel" 
                   text="GAME OVER" 
                   styleClass="main-title"/>
            
            <!-- Rank Display - Positioned below title -->
            <Label fx:id="rankLabel" 
                   text="" 
                   styleClass="rank-display"
                   visible="false">
                <VBox.margin>
                    <Insets top="0" bottom="0" left="0" right="0"/>
                </VBox.margin>
            </Label>
            
            <!-- Subtitle (for new high score) - Hidden -->
            <Label fx:id="subtitleLabel" 
                   text="" 
                   styleClass="subtitle"
                   visible="false"/>
        </VBox>
    </top>
    
    <!-- Center: Score and Leaderboard Section -->
    <center>
        <VBox alignment="CENTER" spacing="10" styleClass="center-section" maxWidth="800.0">
            <padding>
                <Insets top="-12" bottom="10" left="40" right="40"/>
            </padding>
            
            <!-- Your Score Card -->
            <VBox fx:id="yourScoreCard" 
                  alignment="CENTER" 
                  spacing="5" 
                  styleClass="score-card">
                <VBox.margin>
                    <Insets top="10"/>
                </VBox.margin>
                <padding>
                    <Insets top="12" bottom="12" left="20" right="20"/>
                </padding>
                
                <Label text="YOUR SCORE" styleClass="card-title"/>
                <Label fx:id="finalScoreLabel" 
                       text="0" 
                       styleClass="final-score"/>
                
                <HBox spacing="30" alignment="CENTER">
                    <VBox alignment="CENTER" spacing="5">
                        <Label text="LINES" styleClass="stat-label"/>
                        <Label fx:id="linesLabel" text="0" styleClass="stat-value"/>
                    </VBox>
                    <VBox alignment="CENTER" spacing="5">
                        <Label text="LEVEL" styleClass="stat-label"/>
                        <Label fx:id="levelLabel" text="1" styleClass="stat-value"/>
                    </VBox>
                    <VBox alignment="CENTER" spacing="5">
                        <Label text="TIME" styleClass="stat-label"/>
                        <Label fx:id="timeLabel" text="00:00" styleClass="stat-value"/>
                    </VBox>
                </HBox>
            </VBox>
            
            <!-- Leaderboard Section -->
            <VBox fx:id="leaderboardSection" 
                  alignment="CENTER" 
                  spacing="5" 
                  styleClass="leaderboard-section">
                <padding>
                    <Insets top="12" bottom="12" left="20" right="20"/>
                </padding>
                
                <Label text="🏆 LEADERBOARD 🏆" styleClass="leaderboard-title"/>
                
                <VBox fx:id="leaderboardContainer" 
                      alignment="CENTER" 
                      spacing="8"
                      styleClass="leaderboard-container"/>
            </VBox>
        </VBox>
    </center>
    
    <!-- Bottom: Action Buttons -->
    <bottom>
        <VBox alignment="CENTER" spacing="5" styleClass="button-section">
            <padding>
                <Insets top="5" bottom="15" left="40" right="40"/>
            </padding>
            
            <HBox spacing="20" alignment="CENTER">
                <Button fx:id="tryAgainButton" 
                        text="🔄 TRY AGAIN" 
                        styleClass="action-button try-again-btn"
                        mnemonicParsing="false"
                        onAction="#onTryAgain"/>

                <Button fx:id="resetLeaderboardButton" 
                        text="🕓 RESET LEADERBOARD" 
                        styleClass="action-button reset-btn"
                        mnemonicParsing="false"
                        onAction="#onResetLeaderboard"/>

                <Button fx:id="backToMenuButton" 
                        text="🏠 Back TO MENU" 
                        styleClass="action-button menu-btn"
                        onAction="#onBackToMenu"/>
            </HBox>
            
            <!-- Additional Info -->
            <Label text="Press N to try again or ESC to return to menu" 
                   styleClass="hint-text"/>
        </VBox>
    </bottom>
    
</BorderPane>
